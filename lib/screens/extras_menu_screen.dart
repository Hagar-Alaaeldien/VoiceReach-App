import 'package:flutter/material.dart';

class ExtrasMenuScreen extends StatelessWidget {
  const ExtrasMenuScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Extras Menu'),
      ),
      body: const Center(
        child: Text('Extras Menu Screen Placeholder'),
      ),
    );
  }
}
